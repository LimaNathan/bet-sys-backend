package com.coticbet.domain.enums;

/**
 * Type of bet: single or multiple (parlay)
 */
public enum BetType {
    SINGLE, // Single bet on one event
    MULTIPLE // Parlay/combo bet on multiple events
}
